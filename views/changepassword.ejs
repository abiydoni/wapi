<!DOCTYPE html>
<html>
<head>
  <title>Change Password - WhatsApp Gateway</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <!-- Tailwind CDN -->
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-[#fcf5eb] min-h-screen flex flex-col items-center justify-center">
  <main class="w-full flex flex-col items-center justify-center">
    <%- include('partials/topnav', { user: user }) %>
    <div class="w-full max-w-md bg-white/90 backdrop-blur-sm rounded-[25px] shadow-xl border border-gray-800 p-8">
      <h2 class="text-2xl font-bold text-center text-[#3be676] mb-6">Change Password</h2>
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-2 rounded mb-4 text-center">
          <%= error %>
        </div>
      <% } %>
      <% if (typeof success !== 'undefined' && success) { %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-2 rounded mb-4 text-center">
          <%= success %>
        </div>
      <% } %>
      <form action="/changepassword" method="post" class="flex flex-col gap-4">
        <div>
          <label for="oldPassword" class="block text-gray-600 font-medium mb-1">Old Password</label>
          <input type="password" id="oldPassword" name="oldPassword" required class="w-full border border-gray-800 rounded-full px-4 py-2" placeholder="Enter your old password">
        </div>
        <div>
          <label for="newPassword" class="block text-gray-600 font-medium mb-1">New Password</label>
          <input type="password" id="newPassword" name="newPassword" required class="w-full border border-gray-800 rounded-full px-4 py-2" placeholder="Enter your new password">
        </div>
        <div>
          <label for="confirmNewPassword" class="block text-gray-600 font-medium mb-1">Confirm New Password</label>
          <input type="password" id="confirmNewPassword" name="confirmNewPassword" required class="w-full border border-gray-800 rounded-full px-4 py-2" placeholder="Confirm your new password">
        </div>
        <button type="submit" class="bg-[#3be676] hover:bg-[#2ecc60] text-white font-semibold py-2.5 px-4 rounded-full transition-all transform hover:scale-[1.02] active:scale-95 border border-gray-800 shadow-sm hover:shadow-md cursor-pointer mt-2">Change Password</button>
      </form>
    </div>
  </main>
</body>
</html> 